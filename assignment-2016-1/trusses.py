
g=[]
g = open(input("please enter a file"),'r')
edge = g.readline(a,b)
for line=() in g:
   g.append(line.strip())

k = int(input("Please enter the size of truss"))
adjList = []
def find_k_truss(g,k):
   for edge=(a,b) in g:
       if  (size(intersection(neighbours(a), neighbours(b)) < k - 2:
               g.remove(edge)
               return g
       elif adjlist.append(edge))
         print("Print all adjacency lists with corresponding vertex")
            n = len(adjLists)
            for v in range(0,n):
                print(v, ":", adjLists[v])








